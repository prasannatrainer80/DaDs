

Course(CourseId,CourseName,TrainerName,Fee,Status)

Create Table Course
(
   course_Id int,
   courseName varchar(5),
   TrainerName INT,
   Fee varchar(30),
   Status INT
);

-- Add Primary Not Auto_INcrement on Course_Id 
-- Change courseName to varchar(20),
-- Change trainerName to varchar(20)
-- Change fee to numeric(9,2)
